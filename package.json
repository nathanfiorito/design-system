{
    "name": "design-system",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*",
        "configs/*"
    ],
    "scripts": {
        "build": "npm run build -w @ds/tokens && npm run build -w @ds/theme && npm run build -w @ds/ui",
        "test": "npm run test --workspaces"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "prettier": "latest"
    }
}